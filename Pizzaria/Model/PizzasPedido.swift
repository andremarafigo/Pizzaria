//
//  PizzasPedido.swift
//  Pizzaria
//
//  Created by ALUNO on 18/03/19.
//  Copyright © 2019 ALUNO. All rights reserved.
//

/*import Foundation

import Foundation
import CoreData
import UIKit

class PizzasPedidos {

    var pizzasPedido: [PizzasPedido] = []
    //var listaPizzas = Pizzas()
    //var listaPedidos = Pedidos()
    
    /*
    init() {
        let contexto = (UIApplication.shared.delegate as? AppDelegate)!.persistentContainer.viewContext
        let pizzaPedido: PizzasPedido = PizzasPedido(context: contexto)

        pizzaPedido.pedido = self.listaPedidos.pedidos[0]
        pizzaPedido.pizza = self.listaPizzas.pizzas[0]
        pizzaPedido.nPizza = 1
        pizzaPedido.divisao = 3
        pizzaPedido.quantidade = 1
        pizzasPedido.append(pizzaPedido)
        
        pizzaPedido.pedido = self.listaPedidos.pedidos[0]
        pizzaPedido.pizza = self.listaPizzas.pizzas[1]
        pizzaPedido.nPizza = 1
        pizzaPedido.divisao = 3
        pizzaPedido.quantidade = 1
        pizzasPedido.append(pizzaPedido)
        
        pizzaPedido.pedido = self.listaPedidos.pedidos[0]
        pizzaPedido.pizza = listaPizzas.pizzas[2]
        pizzaPedido.nPizza = 1
        pizzaPedido.divisao = 3
        pizzaPedido.quantidade = 1
        pizzasPedido.append(pizzaPedido)
        
        pizzaPedido.pedido = self.listaPedidos.pedidos[1]
        pizzaPedido.pizza = self.listaPizzas.pizzas[0]
        pizzaPedido.nPizza = 2
        pizzaPedido.divisao = 1
        pizzaPedido.quantidade = 1
        pizzasPedido.append(pizzaPedido)
    }*/
}*/
