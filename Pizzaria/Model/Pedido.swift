//
//  Pedido.swift
//  Pizzaria
//
//  Created by André Marafigo on 17/03/19.
//  Copyright © 2019 ALUNO. All rights reserved.
//

/*import Foundation
import CoreData
import UIKit

class Pedidos {

    var pedidos: [Pedido] = []
    //var listaClientes = Clientes()
    //var listaPedido = PizzasPedidos()
    
    /*
    init() {
        let contexto = (UIApplication.shared.delegate as? AppDelegate)!.persistentContainer.viewContext
        let pedido: Pedido = Pedido(context: contexto)
        
        pedido.dataHora = Date()
        pedido.cliente = listaClientes.clientes[0]
        
        for x in listaPedido.pizzasPedido {
            if x.pedido == pedido{
                pedido.valorTotal += ((x.pizza!.valor / Double(x.divisao)) * Double(x.quantidade))
            }
        }
        pedidos.append(pedido)
        
        
        pedido.dataHora = Date()
        pedido.cliente = listaClientes.clientes[1]
        
        for x in listaPedido.pizzasPedido {
            if x.pedido == pedido{
                pedido.valorTotal += ((x.pizza!.valor / Double(x.divisao)) * Double(x.quantidade))
            }
        }
        pedidos.append(pedido)
    }*/
}*/
